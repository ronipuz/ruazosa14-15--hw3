<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/title"
        style="@style/screen_title"
        android:text="@string/main_title" />

    <Button
        android:id="@+id/launch_chance_calculator"
        style="@style/main_button"
        android:text="@string/main_calculate_chances" />

    <Button
        android:id="@+id/launch_ticket_creator"
        style="@style/main_button"
        android:text="@string/main_build_ticket" />

    <Button
        android:id="@+id/launch_wheel_of_fortune"
        style="@style/main_button"
        android:text="@string/main_spin_wheel" />

</LinearLayout>
